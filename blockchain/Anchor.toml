[toolchain]
anchor_version = "1.0.2"
package_manager = "yarn"

[features]
resolution = true
skip-lint = false

[programs.devnet]
baseline_program = "4tSkA5NcQAerpErKUukc3N9m5Mzyih9xLtyoskrsEsVu"

[provider]
cluster = "devnet"
wallet = "C:/Users/Stella/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"

[hooks]
